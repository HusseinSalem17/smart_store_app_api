import 'package:flutter/material.dart';

class AuthenticatingScreen extends StatelessWidget {
  const AuthenticatingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Authenticating Screen'),
    );
  }
}
